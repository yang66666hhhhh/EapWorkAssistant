# P1 重构完成报告

> 时间：2026-08-29 21:11–23:50（含 22:38 起的续作）
> 范围：评审报告中的三项 P1（数据库迁移 / 依赖注入 / 巨型类拆分）

## 结果总览

| 项 | 内容 | 状态 |
|----|------|------|
| P1-3 | 数据库迁移版本化 + 空值防护 | ✅ 完成 |
| P1-2 | 引入 DI 容器，消除 `new()` 与静态单例耦合 | ✅ 完成 |
| P1-1 | 拆分巨型类（4 个职责已抽出并补测试） | ✅ 完成（部分） |
| 验证 | 构建 0 错误；测试 **91/91 通过** | ✅ |

`WorkRecordViewModel` **1337 → 1260 行**；新增单元测试 **47 个**（导入 18 + 分页 21 + 趋势 8）。

> **验证**：全量 `dotnet test` 跑通 **91/91，约 21s**（之前几次因磁盘 IO 劣化未跑完，
> 环境恢复后一次通过）。期间唯一出现过的 2 个失败是测试断言切片越界，已修正，非实现缺陷。
>
> **已提交**：`c394122 refactor: P1 重构 — 数据库迁移版本化 + 依赖注入 + 巨型类拆分`
> （21 files，+1562/−346，pre-commit 钩子通过）。**仅本地提交，未推送**——其他里程碑的
> 未跟踪文件（Badge/SearchBox/PrimaryActionButton/outputs 等）未纳入本提交。

---

## P1-3 数据库迁移版本化

**文件**：`Data/DatabaseInitializer.cs`（261 → 388 行）

- 用 **PRAGMA user_version**（当前 v8）+ `PRAGMA table_info` 幂等前置检查，
  替换原先约 15 段 `try { ALTER TABLE } catch { }`「靠吞异常判断列是否存在」的写法。
- 建表语句改为**最新完整 schema**，全新库走「建表 → 直接置为 v8」快路径，不再跑任何 ALTER。
- 迁移失败改为**写日志并中止后续迁移**（版本号停在最后成功版本），不再静默吞掉；
  不向上抛出，避免启动即崩溃。
- **修复潜在崩溃**：backfill 中 `reader.GetString()` 对可为 NULL 的 TEXT 列未判空 →
  改用 `SafeString / SafeDouble / SafeInt`（IsDBNull 防护）。

**踩坑记录**：「是否全新库」的判断必须在 `CreateTables()` **之前**——建表后表必然存在，
判断恒为 false。首次实现就踩了这个坑，已修正。

新增迁移的正确姿势：往 `Migrations` 列表**追加**条目并递增 `LatestSchemaVersion`；
已发布的迁移只能追加、不得修改，否则历史库与新库结构分叉。

## P1-2 依赖注入

**新增**：`Services/ServiceContainer.cs`（124 行）；引用 `Microsoft.Extensions.DependencyInjection` 10.0.11

- 组合根集中注册 4 个 Repository（单例）、各服务、以及**子 ViewModel**（单例）。
- ViewModel 统一采用**双构造模式**：
  ```csharp
  public X() : this(ServiceContainer.Get<T>()) { }  // 保住 XAML 需要的无参构造
  public X(T dep) { ... }                            // 给单测留注入口
  ```
- **MainViewModel 刻意不在容器注册**：它由 `MainWindow.xaml` 的 `<vm:MainViewModel/>` 实例化，
  若容器里也注册一份就会变成两个实例、导致导航状态分裂。子 VM 由 MainViewModel 从容器解析。
- `SettingsViewModel` 中 6 处 `new WorkRecordRepository()` / `new KnowledgeRepository()` 改为注入字段。
- `App.OnStartup` 增加 `ValidateServiceContainer()` 自检：只解析仓储与既有单例（无副作用），
  不解析 ViewModel（避免提前触发定时器）。

**踩坑记录**：`HolidayService` 构造子是 **private**，只能 `AddSingleton(HolidayService.Instance)`；
按类型 `AddSingleton<HolidayService>()` **编译能过，但运行时解析才炸**（DI 容器默认不做可解析性校验）。

## P1-1 拆分巨型类

**新增**：`Services/WorkRecordImportService.cs`（131 行）、`EapWorkAssistant.Tests/WorkRecordImportServiceTests.cs`

- 把 `WorkRecordViewModel.ImportCsvAsync` 中的**纯逻辑**抽成 `WorkRecordImportService`
  （`Validate` 校验 + `CheckConfig` 配置一致性检查）。
- 该服务刻意**不引用 ConfigService / DialogService**：已知配置项由调用方以集合传入，
  因此是纯函数、可直接单元测试。
- 新增 18 个用例：日期格式拦截（`2026/8/3`、Excel 序列号）、必填字段、工时边界（0.5/24 通过，24.5 拒绝）、
  进度越界截断、部分失败不影响其余记录、配置一致性、预览超 5 项截断。
### 续作（22:38 起）又抽出两个职责

**新增**：`Services/PaginationCalculator.cs`（74 行，纯静态）、`Services/CompLeaveBalanceService.cs`（74 行）

- **分页算术** → `PaginationCalculator`（`CalculateTotalPages` / `ClampPage` / `BuildPageText` /
  `BuildVisiblePageNumbers`，约定 `0` 为省略号）。原先是 VM 里三个私有方法；
  页码算错是分页控件最容易回归的地方，抽出后用 21 个用例锁死行为
  （总页数边界 20/21、当前页越界收敛、首末窗口折叠、省略号位置等）。
- **调休余额** → `CompLeaveBalanceService`：汇总全年工时 + "调休"类请假，再交给
  `CompLeaveCalculator`；**内部自行确保假日数据已加载**（`LoadYearAsync` 幂等），
  "数据是否可用"由调用方查询后决定如何提示，从而保持本类无 UI 依赖。
- 报告生成三个命令（日报/周报/月报）**评估后刻意不拆**：本身只是 3–8 行的转发壳，
  拆出去要把事件与 Toast 一起搬走，反而更绕——不是所有"大文件里的代码"都该拆。

`WorkRecordViewModel` 最终 **1337 → 1260 行**。

> ⚠️ **诚实说明**：行数净减 77 行（新增构造与注释样板吃掉不少），
> 但**可测性与职责边界是实质改善**——原先完全无法单测的三块逻辑现在有 39 个用例覆盖。

---

## 遗留 / 待办

1. **巨型类未拆完**：`WorkRecordViewModel` 仍 1260 行，剩余待拆为报告生成（已评估为低价值）、
   请假记录管理、日历状态加载；`DashboardViewModel`(874)、`SettingsViewModel`(735)、
   `DataGridCopyHelper`(785) 完全未动。
2. **`EapWorkAssistant.Tests/bin/` 下仍有约 20 个 `_verify_*` 目录（约 6–7GB）**：
   已删掉一半；safe-delete 走回收站，单个 328MB 很慢（一轮约 10 分钟）。
   **教训：删大目录时不要与构建/测试并发**，IO 争用会让构建从 30s 劣化到数分钟，
   并诱发一堆 "Access is denied" 假性报错。
3. P2/P3 项（后台线程异常兜底、搜索硬编码中文串、SQLite 连接池、Dashboard 聚合查询合并、
   `ThemeService.GetChartColors` 强转防护等）未动，详见 `项目评审报告.md`。

## 验证命令

```bash
# 构建（若 IDE 持锁 obj\Debug，用下面的隔离方式）
dotnet build EapWorkAssistant.csproj -c Debug
dotnet build EapWorkAssistant.csproj -c Debug -p:BaseIntermediateOutputPath=obj/_verify_x/ -p:OutputPath=bin/_verify_x/

# 测试（建议环境空闲时跑，确认 83 例全绿）
dotnet test EapWorkAssistant.Tests/EapWorkAssistant.Tests.csproj -c Debug -p:OutDir=bin/_verify_test/
# 预期：已通过! 失败: 0，通过: 83

# 单独验证分页用例（约 1 分钟）
dotnet test EapWorkAssistant.Tests/EapWorkAssistant.Tests.csproj -c Debug \
  -p:BaseIntermediateOutputPath=obj/_verify_p/ -p:OutputPath=bin/_verify_p/ \
  --filter "FullyQualifiedName~PaginationCalculatorTests"
```

## 改动文件

- 修改（10）：`App.xaml.cs`、`Data/DatabaseInitializer.cs`、`EapWorkAssistant.csproj`、
  `ViewModels/{Main,WorkRecord,Dashboard,Knowledge,Issue,RecycleBin,Settings}ViewModel.cs`
- 新增（3 源码）：`Services/ServiceContainer.cs`、`Services/WorkRecordImportService.cs`、
  `EapWorkAssistant.Tests/WorkRecordImportServiceTests.cs`

**尚未提交 git**（沿用你分里程碑提交的节奏，等你确认后再 commit）。
