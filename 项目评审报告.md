# EAP Work Assistant v2.1 — 项目评审报告

> 评审范围：`E:\Myproject\EapWorkAssistant` 全部源码（约 80 个 .cs 文件，不含 bin/obj）。
> 评审方式：静态代码审阅（未运行构建/测试，因请求为评审而非诊断）。

## 一、整体结论

这是一个**功能完整、可用性强**的 WPF 桌面应用：试用期工程师工作助手，覆盖工作记录、知识库、问题跟踪、仪表盘、报告生成、CSV 导入导出、全局搜索、主题系统等。**架构方向是对的**（MVVM + Repository + SQLite/Dapper + LiveCharts2），基础防护（参数化 SQL、软删除、定时备份、CSV 二进制嗅探、防抖搜索）都到位。

但代码在**可维护性、可测试性、架构边界、工程规范**上存在明显短板，最突出的是「巨型 ViewModel」和「全局静态单例耦合」。

---

## 二、已做到的好设计（值得保留）

- **安全查询**：`WorkRecordRepository` 的 SQL 全部参数化；唯一动态拼接的列名 `sortColumn` 用白名单 `AllowedSortColumns` 校验，无 SQL 注入风险。
- **CSV 导入防呆**：`ExportService.GuardAgainstNonCsvFile` 同时做了扩展名 + 文件头魔数嗅探（ZIP/OLE2），能挡住「改后缀的 Excel」，避免乱码污染数据库。
- **导入的幂等去重**：`UniqueId` 业务键 + 回填逻辑，使「导出→导入」不产生重复；`ImportMode` 区分跳过/覆盖/追加。
- **主题系统完整**：双主题 + 强调色 + 字号 + 密度，且针对 LiveCharts/Skia 无法用 DynamicResource 的情况做了手动取色重绘（`GetChartColors`）。
- **健壮性兜底**：`App.xaml.cs` 三处未处理异常钩子；测试工程与产物目录做了隔离配置。

---

## 三、主要不足（按严重程度）

### P1 — 架构与可维护性（高优先）

1. **巨型 ViewModel / 上帝类**
   - `WorkRecordViewModel.cs` **1337 行**、`DashboardViewModel.cs` 874 行、`SettingsViewModel.cs` 735 行、`Helpers/DataGridCopyHelper.cs` 785 行。
   - `Work 记录` 的 ViewModel 同时承担了：UI 状态、三个定时器（状态/搜索/自动保存）、CRUD、CSV 导入流程、报告生成、请假余额、日历状态……职责过重。超过 8 个独立关注点塞在一个类里，可读性、单测、回归都受影响。
   - **建议**：按职责拆分（如 `WorkRecordImportService`、`ReportGenerator`、`LeaveBalanceService`），ViewModel 只做状态编排。

2. **全局静态单例 + new() 耦合**
   - `ConfigService.Instance`、`ThemeService.Instance`、`ToastService.Instance`、`DialogService.Instance` 以及 `DatabaseInitializer` 静态类，Repository 在 VM 内 `new()` 直接实例化。
   - 无依赖注入容器，导致：① 无法替换实现做单元测试；② 跨模块隐式依赖（VM ↔ Service ↔ 静态配置）；③ Repository 无法复用连接/事务。
   - **建议**：引入轻量容器（Microsoft.Extensions.DependencyInjection），将 Repository/Service 注入 VM；配置/主题/Toast 也可改为注入。

3. **数据库迁移缺乏版本管理**
   - `DatabaseInitializer` 用 `try { ALTER TABLE ... } catch { }` 逐个字段「吞掉异常」来做迁移（约 15 段 try/catch）。缺点：真实错误会被静默忽略（列名写错、磁盘满等都变成「列已存在」假象）；没有 schema 版本号，无法做结构化升级/回滚。
   - `WorkDate` 等 TEXT 列允许 NULL，回填分支 `reader.GetString(3)`（`Content`）未判空，历史脏数据可能为 NULL → 运行时 `InvalidCastException`（潜在崩溃点，位于 DatabaseInitializer.cs:234-241）。
   － **建议**：用成熟迁移方案（如自建 `schema_version` 表）+ try/catch 记录日志而非吞掉；读取字段统一用 `IsDBNull` 防护。

### P2 — 健壮性与正确性（中优先）

4. **后台线程异常兜底不完整**
   - `App.xaml.cs` 的 `OnDomainUnhandledException` 未设置 `e.Handled`，且其中调用 `ToastService`（非 UI 线程调用 UI 弹窗本身易失败 —— 虽然包了 try/catch，但兜底日志路径若失败也会清空线程）。后台 Task 未 await 的异常仍可能在 finalization 时终止进程。
   - **建议**：区分 UI/非 UI 线程异常，后台线程优先写日志；必要时用 `Dispatcher` 回 UI 线程弹 Toast。

5. **搜索分类用硬编码中文串做筛选**
   - `MainViewModel.SearchAsync` 用 `"工作记录"/"知识库"/"问题跟踪"` 同时作为导航键和计数过滤条件。文案一旦在 UI 改动，计数逻辑会静默失效。
   - **建议**：抽成常量/枚举（`SearchType.Record` 等）。

6. **SQLite 连接未启用连接池 / 复用**
   - 几乎每个查询 `new SQLiteConnection(...)` 后立刻 Open/Close。SQLite 默认未开启连接池（`Pooling=true`），高频切换视图会有开销；且字符串日期比较（`BETWEEN`/`LIKE`）不如索引化的真实日期类型高效。
   - **建议**：开启 `Pooling=true` 或复用单连接（配合事务）；时间字段已是统一 `yyyy-MM-dd` 文本（当前良好）。

7. **DashboardViewModel 潜在的 N+1 / 多次查询**
   - 仪表盘统计会并发调用多个聚合查询，部分可合并为单条 SQL（如项目分布 + 类型分布可在一次查询中完成）。当前散落多处异步，存在多轮往返。

### P3 — 工程规范（低优先，但累积成本）

8. **`DataGridCopyHelper` 785 行**：复制辅助类异常庞大，疑似包含大量样板/重复逻辑，建议拆分或下沉到基础控件。
9. **重复的依赖列表与魔法字符串**：多个 Repository 几乎相同的 `Task.Run + OpenAsync + Query` 模板可抽象为基类 `SqliteRepository`。
10. **`ThemeService.GetChartColors` 直接 `(Color)res["X"]` 强转**：若资源缺失会抛 `InvalidCastException`（其他 SetColor 有 `Contains` 保护，此处没有）。
11. **无 `.editorconfig` / Helper 目录扁平**：40+ 个 Converter/Helper 散落，建议按子领域归类。

---

## 四、未解决 / 需注意

- 我**未运行构建与测试**（评审请求，非诊断）；记忆记录显示 44 项测试历史全绿，但你本地跑 `dotnet test -p:OutDir=bin/_verify_test/` 可确认当前状态。
- 个人文件安全规则：本次仅读取与审阅，未对任何目录做删除/移动。
- 若后续要做重构（拆分 ViewModel、引入 DI），建议先建自动化回归测试覆盖范围，避免回归。

## 五、下一步建议（可按需让我执行）

1. **立即**：把 `DatabaseInitializer` 的 try/catch 改为带日志的迁移表机制，并修复 `GetString` 空值防护（防崩溃）。
2. **中期**：引入 DI 容器，拆分 `WorkRecordViewModel` 与 `SettingsViewModel`。
3. **长期**：建立 `SchemaVersion` 迁移表、合并 Dashboard 聚合查询、用枚举替换搜索硬编码串。

如需，我可以创建「瘦身 WorkRecordViewModel / 数据库迁移」的分步计划，或直接在 Craft 模式下动手改造其中某一项。
